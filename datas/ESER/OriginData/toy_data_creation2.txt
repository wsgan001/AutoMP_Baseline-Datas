train后缀（由5到40）改为 1到8
候选点以及负例限定在距离中心4步以内（这已经很大了），但是不需要抽取子图了，只需要给出采样的路径即可。
负例改为随机选取同类型，训练文件每行的负例都随机选，每个训练文件都100行数据.
subpath为从中心出发随机采(4步以内)，即一旦到达（与例子）同类型的节点即终止并保留路径（可重复）。每组查询固定采样10万次（采到的数量不一定），其中一半逆序表示。
生成一个新的nodefeature文件，用以存储整个DBpedia每个节点的特征，维度2*（DBpedia总type数+1）具体格式如下：
第一行：节点总数N 特征维度n
其余行：id(从0递增到N-1) f1 f2 ... fn   // n维特征向量
共 N+1 行，空格分割，行末只有换行符。
